<form method="post" action="{{ path('app_artist_delete', {'artistId': artist.artistId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artist.artistId) }}">
    <button class="btn btn-outline-danger mt-2">Supprimer</button>
</form>
